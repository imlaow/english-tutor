# Current Active Task

**Agent Instruction**: You are allowed to read this file and ARCHITECTURE.md only. You must execute ONLY the task listed below. Once finished, stop immediately and wait for user review. DO NOT create, plan, or execute any other tasks.

- [ ] **Task 2**: Setup the Room Database class (`AppDatabase`) and implement the `ProfileRepository` inside the `data/repository` package. Use Manual DI concepts. The Repository should expose methods to save and retrieve the UserProfileEntity.
- 