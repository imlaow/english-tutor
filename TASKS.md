# Current Active Task

**Agent Instruction**: You are allowed to read this file and ARCHITECTURE.md only. You must execute ONLY the task listed below. Once finished, stop immediately and wait for user review. DO NOT create, plan, or execute any other tasks.

- [ ] **Task 1**: Create the Room Database Entity (`UserProfileEntity`) and DAO (`UserDao`) inside the `data/local` package. The entity fields must include: english_level (String), learning_goal (String), topics_of_interest (List<String> or String), and daily_practice_time (Int). Ensure the proper Room annotations are used. Do not write any network, Repository, or UI code.
