*.iml
.gradle
.kotlin
/local.properties

# IDE state. Tracked until 2026-08-10 — it churns on every IDE action, and
# deploymentTargetSelector.xml even stored an ADB endpoint.
.idea/

# Local simulation state for `wrangler r2` and friends.
.wrangler/

# Which debug APK scripts/publish-debug.sh last pushed to R2, so the next run
# knows what to delete. Per-machine state, not a shared fact.
/.publish-debug-state
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
debug.keystore
TASKS.md

# Release signing key — never commit
*.jks
*.keystore
keystore.properties
